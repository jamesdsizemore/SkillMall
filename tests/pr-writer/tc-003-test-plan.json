{
  "id": "tc-003",
  "description": "Includes a specific test plan with verification steps",
  "input": "Write a PR description for adding rate limiting to the API: 100 requests per minute per user, returns 429 with Retry-After header.",
  "required": [
    "test plan with specific steps",
    "429 status code verification",
    "Retry-After header mentioned"
  ],
  "forbidden": [
    "test manually",
    "no test plan"
  ]
}
