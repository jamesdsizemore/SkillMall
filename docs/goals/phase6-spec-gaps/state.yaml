version: 2
active_task: null

goal:
  title: "Phase 6 — Spec Gap Closure"
  slug: phase6-spec-gaps
  status: done
  created_at: "2026-05-18"
  goal_file: docs/goals/phase6-spec-gaps/goal.md
  intake:
    input_shape: existing_plan
    authority: approved
    proof_type: demo
    existing_plan_facts:
      - "8 gaps across wizard, budget analyzer, dependency graph, and minor polish"
      - "N1+N2 are in the same wizard component files — one Worker"
      - "N3 requires reading lib/budget-analyzer.ts first to understand char-to-agent mapping"
      - "N4+N5+N6 are all graph — one Worker to avoid file conflicts"
      - "N7+N8 are minor polish — one Worker"
      - "Spec §§3.1, 4.2, 4.3, 5.3, 2.3 are implementation source of truth"

agents:
  scout: unknown
  worker: unknown
  judge: unknown

tasks:
  - id: T601
    type: worker
    assignee: Worker
    status: done
    objective: "N1+N2: Step4Preview inline editing + char counter. Step6Confirm expand-to-preview."
    allowed_files:
      - components/skill-mall/wizard/Step4Preview.tsx
      - components/skill-mall/wizard/Step6Confirm.tsx
      - components/skill-mall/wizard/wizard-reducer.ts
    verify:
      - cmd: "npx tsc --noEmit"
        expect: exit 0
    stop_if:
      - "tsc fails after two repair attempts."
    receipt:
      result: done
      changed_files:
        - components/skill-mall/wizard/Step4Preview.tsx
        - components/skill-mall/wizard/Step6Confirm.tsx
      commands:
        - cmd: "npx tsc --noEmit"
          status: pass
        - cmd: "npm test"
          status: pass
      summary: "N1: Step4 textarea + live 1024-char description counter. N2: Step6 expand-to-preview per file."

  - id: T602
    type: worker
    assignee: Worker
    status: done
    objective: "N3: Budget Analyzer agent dropdown + 4-level table at 10/20/30/50 installed skills."
    allowed_files:
      - lib/budget-analyzer.ts
      - components/skill-mall/skill-detail/SkillTabs.tsx
      - app/api/budget-check/route.ts
    verify:
      - cmd: "npx tsc --noEmit"
        expect: exit 0
    stop_if:
      - "tsc fails after two repair attempts."
    receipt:
      result: done
      changed_files:
        - lib/budget-analyzer.ts
        - app/api/budget-check/route.ts
        - components/skill-mall/skill-detail/SkillTabs.tsx
      commands:
        - cmd: "npx tsc --noEmit"
          status: pass
      summary: "N3: Agent dropdown (6 agents) with per-agent budget tables. API now supports ?agent= param. BudgetPanel shows 4-row table at 10/20/30/50 installed skills."

  - id: T603
    type: worker
    assignee: Worker
    status: done
    objective: "N4+N5+N6: Graph category filter, hover tooltip, cluster detection."
    allowed_files:
      - lib/graph.ts
      - components/skill-mall/graph/DependencyGraph.tsx
      - components/skill-mall/graph/GraphNode.tsx
      - app/graph/page.tsx
    verify:
      - cmd: "npx tsc --noEmit"
        expect: exit 0
    stop_if:
      - "tsc fails after two repair attempts."
    receipt:
      result: done
      changed_files:
        - lib/graph.ts
        - components/skill-mall/graph/DependencyGraph.tsx
        - components/skill-mall/graph/GraphNode.tsx
        - app/graph/page.tsx
      commands:
        - cmd: "npx tsc --noEmit"
          status: pass
      summary: "N4: category filter buttons toggle nodes/edges. N5: hover title tooltip via native title attr. N6: cluster detection — 5+ internally-connected nodes get amber ring and CLUSTER label."

  - id: T604
    type: worker
    assignee: Worker
    status: done
    objective: "N7+N8: Most Forked badge + artifact-type-specific framework defaults."
    allowed_files:
      - app/skills/[category]/[slug]/page.tsx
      - components/skill-mall/skill-detail/FrameworkPicker.tsx
      - components/skill-mall/skill-detail/PromptCard.tsx
    verify:
      - cmd: "npx tsc --noEmit"
        expect: exit 0
    stop_if:
      - "tsc fails after two repair attempts."
    receipt:
      result: done
      changed_files:
        - app/skills/[category]/[slug]/page.tsx
        - components/skill-mall/skill-detail/FrameworkPicker.tsx
        - components/skill-mall/skill-detail/PromptCard.tsx
      commands:
        - cmd: "npx tsc --noEmit"
          status: pass
        - cmd: "npm test"
          status: pass
      summary: "N7: Most Forked badge at forkCount>=5. N8: FrameworkPicker defaults mapped per artifact type (matrix/canvas/grid/analysis/flowchart/list); PromptCard infers type from produces filenames."

  - id: T605
    type: judge
    assignee: Judge
    status: done
    objective: "Final audit: verify all 8 gaps (N1–N8) are closed per spec."
    inputs:
      - "All T601–T604 receipts"
      - "npx tsc --noEmit"
      - "npm test output"
      - "git diff --stat"
    expected_output:
      - "complete | not_complete per gap"
      - "full_outcome_complete: true | false"
    receipt:
      result: done
      decision: complete
      full_outcome_complete: true
      rationale: "All 8 spec gaps closed in commit b111c89. N1-N8 verified. tsc clean, 211/211 tests. Feature inventory gap count: 0."
